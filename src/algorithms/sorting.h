#ifndef SORTING_H
#define SORTING_H

namespace sorting {
  void bubble_sort(int arr[], int n);
  void insertion_sort(int arr[], int n);
  void merge(int arr[], int p, int q, int r); // TODO: REMOVE FROM HEADER
  void merge_sort(int arr[], int n);
}

#endif // !SORTING_H
